name: Vars Testing

on:
  push:
    branches:
      - vars

jobs:
  display-variables:
    name: display-variables
    runs-on: ${{ vars.GH_RUNNER_TAG }}
    environment: development
    steps:
    - name: Use variables
      run: |
        echo "Repository variable : ${{ vars.GH_RUNNER_TAG }}"
